/**
 * Contains components responsible for traversing object graphs using property paths.
 *
 * <p>This package provides:</p>
 * <ul>
 *   <li>{@code PathTraverser} - Core logic for navigating through object properties</li>
 *   <li>{@code PathTraversalResult} - Represents the result of a path traversal operation</li>
 * </ul>
 *
 * <p>The traversal system supports:</p>
 * <ul>
 *   <li>Nested property access using dot notation</li>
 *   <li>Automatic null handling</li>
 *   <li>Creation of intermediate objects when needed</li>
 * </ul>
 */
package org.example.reflection.internal.traversal;
